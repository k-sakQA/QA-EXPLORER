# Test Charters — テスト設計(観点リスト形式)

テスト対象: `https://hotel-example-site.takeyaqa.dev/ja/plans.html`
作成日: `2026-09-23`
入力: `risk-register.md` (Approved 2026-09-23, さかた)

**Review-Status: Draft**
<!-- Draft / Approved。エージェントは自分で Approved にしてはならない。 -->

Approved-By: `<名前>`
Approved-Date: `YYYY-MM-DD`

---

## レビュー依頼事項

### 工数超過 — 判断をお願いします

想定ケース数 **53** に対し、`risk-input` 項目8 の目安は **50**。3ケース超過。

削るなら浅いチャーターから。候補は次の2つです。

| 候補 | 削る場合の根拠 | 残す場合の根拠 |
|---|---|---|
| **CH-13 (R-11 予約番号)** | 仕様(会員機能なし)である可能性が高く、テストで壊れる余地が小さい | 仕様か欠落かが未確定のまま承認されている。1ケースで確認だけしておく価値はある |
| **CH-10 (R-06 電話11桁)** | 影響度 L(機会損失は許容)。固定電話の客を弾く設計判断の話で、バグではない | 1ケースなので削っても効果が薄い |

**推奨**: CH-13 を「仕様確認1ケース」に留めたまま残し、超過3ケースは許容する。
53 と 50 の差は誤差の範囲で、削って失うものの方が大きいと判断しました。

### 深さの割当に自信が無いチャーター

- **CH-07 (R-17 / 観点11)** — 発生可能性 L は「明確な兆候が無い」ことが根拠で、
  積極的な根拠ではない。P0観点を明示的に消化するために立てたチャーターなので、
  **4ケースは多いかもしれない**。2ケースに落としても目的は果たせる
- **CH-12 (R-16 / 観点17)** — 「キャンセル導線が無い」こと自体は仕様。
  テストできるのは「離脱時に入力がどう残るか」だけで、それは CH-01 (R-05) と
  重なる。**CH-01 に統合してもよい**かもしれない

### 因子と水準の切り出しに自信が無いチャーター

- **CH-02 (R-04 / サーバ側検証)** — 「外し方」の水準に *直接 GET パラメータを改変* を
  含めた。これは実ユーザーの到達経路ではないが、サーバ側チェックの有無を確かめるには
  これしかない。**やってよいか確認したい**(破壊的リクエストではなく GET のみ)

### 計画(risk-register)とのずれ

なし。13件すべてにチャーターを割り当て、対象外4件には作っていない。

### 抜け落ち候補のうち、未回答のまま設計に進んだもの

なし(4件すべて 2026-09-23 に回答済み)。

---

## チャーター一覧

**「紐づくリスク」が空のチャーターは存在してはならない。**
「影響度の根拠」は `risk-register.md` からの転記で、影響度 L なら `なし(影響度L)` と書く。
**影響度 L は起票しない理由にならない**(リスク一覧が「対象外」にしなかった時点で存在理由がある)。

| 順 | CH-ID | BF | リスク | 影響度の根拠 | 観点 | 狙う欠陥仮定 | 深さ | 技法 | ケース数 | 完了条件 | 状態 |
|:-:|---|:--:|:--:|---|:--:|---|:--:|---|:--:|---|---|
| 1 | CH-01 | BF-01 | R-05 | 項目3-2, 3-6 | 13,10 | ①ブラウザバック後に宿泊日・宿泊数・人数が初期値に戻る ②戻ったことが画面上で分からない ③そのまま送信すると初期値の内容で確定する | 深い | シナリオ + 状態遷移 / ペアワイズ / 境界値 | 6 | 離脱→復帰→送信の一連で、どのフィールドが失われ、失われたまま確定できるかが特定できている | 未実行 |
| 2 | CH-02 | BF-01 | R-04 | 項目3-2, 3-6 | 06,03 | ①maxlength を外すと上限超過が通る ②pattern を外すと不正形式が通る ③min/max を外すと範囲外が通る | 深い | シナリオ / クラシフィケーションツリー→ペアワイズ / 異常値 | 7 | クライアント側の制限ごとに、サーバ側チェックの有無が表で示せている | 未実行 |
| 3 | CH-03 | BF-01 | R-01 | 項目3-2, 3-6 | 23 | ①宿泊数0で確認画面に進める ②人数0で進める ③負数・空で進める | 深い | シナリオ / 境界値 + ペアワイズ | 6 | 宿泊数・人数それぞれの下限・上限の挙動が境界値で確定している | 未実行 |
| 4 | CH-04 | BF-01 | R-02 | 項目3-2, 3-6 | 09 | ①全角スペースのみの氏名で進める ②半角スペース・タブのみでも進める ③前後空白が trim されずに保存される | 深い | シナリオ / 同値分割 + 境界値 | 5 | 空白文字の種類ごとに必須チェックの挙動が確定している | 未実行 |
| 5 | CH-05 | BF-02 | R-09 | 項目3-1, 3-3, 4-2 | 15 | ①人数変更が料金に反映されない ②確認画面から戻って変更すると古い料金が残る ③追加プランの変更が反映されない | 深い | シナリオ + 状態遷移 / ペアワイズ | 6 | 変更操作と反映タイミングの組合せで、表示額と実際の条件がずれる経路の有無が確定している | 未実行 |
| 6 | CH-06 | BF-03 | R-10 | 項目4-1 | 19,12 | ①完了ダイアログを閉じて再クリックすると二重に予約が入る ②連打で二重送信できる ③送信中にボタンが無効化されない | 深い | シナリオ + 状態遷移 / デシジョンテーブル | 6 | 予約確定後にボタンが再度押せるか、押した場合に何が起きるかが確定している | 未実行 |
| 7 | CH-07 | BF-01 | R-17 | 項目3-2 | **11** | ①送信しても確認画面に遷移しない ②遷移するが入力と違う内容が出る ③条件付き必須(email/tel)の切替で状態が壊れる | 標準 | シナリオ / 同値分割 + 境界値 | 4 | 送信イベントによる画面・状態の遷移が、入力内容どおりに起きることが確認できている | 未実行 |
| 8 | CH-08 | BF-01 | R-03 | なし(影響度L) | 06 | ①氏名に長大文字列で414エラー ②何文字から発生するか | 標準 | シナリオ / 境界値 | 3 | 414 が発生する文字数の境界が特定できている | 未実行 |
| 9 | CH-09 | BF-02 | R-08 | 項目3-1, 3-3, 4-2 | 15,18 | ①合計料金の計算式が誤っている ②極端な組合せで計算が破綻する | 標準 | シナリオ / ペアワイズ | 4 | 代表的な組合せで表示額と期待値が一致することが確認できている | 未実行 |
| 10 | CH-10 | BF-01 | R-06 | なし(影響度L) | 05 | ①10桁の固定電話番号が弾かれる | 浅い | スモーク | 1 | 10桁・11桁それぞれの受理／拒否が確認できている | 未実行 |
| 11 | CH-11 | BF-01 | R-07 | なし(影響度L) | 02 | ①エラーメッセージが英語混在 ②フォーカスが不正フィールドに移動しない | 浅い | スモーク | 2 | メッセージの言語とフォーカス移動の実態が記録できている | 未実行 |
| 12 | CH-12 | BF-01 | R-16 | なし(影響度L) | 17 | ①キャンセル相当の操作後に入力が中途半端に残る | 浅い | スモーク | 2 | 離脱操作後のフォーム状態が記録できている | 未実行 |
| 13 | CH-13 | BF-03 | R-11 | 項目4-1 (影響度M) | 19 | ①予約完了後に予約番号・確認手段が一切得られない | 浅い | スモーク(仕様確認) | 1 | 予約の実在を後から確認する手段の有無が確定している | 未実行 |

<!-- 状態: 未実行 / 実行中 / 完了 / 未確認のまま終了 -->

### 深さと技法の対応 (3層)

技法は3層で考える。リスクは業務フロー単位で立てているので、
フィールド単位の技法だけを並べない。

| 深さ | ① フローをどう通すか | ② 組合せをどう作るか | ③ 値をどう選ぶか | ケース数 |
|---|---|---|---|:--:|
| 深い | シナリオ / ユースケース + 状態遷移(Nスイッチ) | クラシフィケーションツリーで因子と水準を切り出し → ペアワイズ / デシジョンテーブル / CFD法 | 同値分割 → 境界値 → 異常値 | 5〜8 |
| 標準 | 主要シナリオ1本 | — | 同値分割 → 境界値(または代表的な異常値) | 3〜5 |
| 浅い | スモーク(主要導線が通ることだけ) | — | — | 1〜2 |

---

## 因子と水準 (深さ「深い」のチャーターのみ)

**ペアワイズもデシジョンテーブルも、因子と水準が決まって初めて使える技法。**
この欄が空のまま組合せケースを作らない。**AIが案を出し、確定するのは人間**。

### CH-01 (R-05 / 離脱後の状態消失)

| 因子 | 水準 | 切り出しの根拠 |
|---|---|---|
| 離脱操作 | ブラウザバック / ロゴリンク経由 / リロード | F-07・F-17 で経路により挙動が違う可能性 |
| 対象フィールド | 宿泊日 / 宿泊数 / 人数 / 氏名 / メール / 追加プラン | F-07 で「保持されるもの」と「戻るもの」が混在 |
| 復帰後の操作 | そのまま送信 / 一部だけ再入力して送信 | 「気づかず確定する」経路を再現するため |

- 禁則(ありえない組合せ): なし
- 導出に使う技法: ペアワイズ
- 導出したケース数: 6

### CH-02 (R-04 / サーバ側検証の欠落)

| 因子 | 水準 | 切り出しの根拠 |
|---|---|---|
| 制限の種類 | maxlength / pattern / min-max / required | F-06・F-14・F-16 で確認済みの制限 |
| 外し方 | DevTools で属性削除 / GET パラメータを直接指定 | フォームが GET 送信のため後者が可能 (F-13) |
| 対象項目 | 氏名 / コメント / 電話 / 宿泊数 | 制限の種類ごとに該当する項目 |

- 禁則: pattern は電話のみ。maxlength は氏名・コメントのみ。min-max は宿泊数・人数のみ
- 導出に使う技法: クラシフィケーションツリーで切り出し → ペアワイズ
- 導出したケース数: 7

### CH-03 (R-01 / 0泊0名)

| 因子 | 水準 | 切り出しの根拠 |
|---|---|---|
| 宿泊数 | -1 / 0 / 1 / 9 / 10 / 空 | F-04・F-06。min 属性が無く上限も不明 |
| 人数 | -1 / 0 / 1 / 9 / 10 / 空 | F-05。同上 |

- 禁則: なし
- 導出に使う技法: 境界値分析 → ペアワイズ
- 導出したケース数: 6

### CH-04 (R-02 / 空白文字のみの氏名)

| 因子 | 水準 | 切り出しの根拠 |
|---|---|---|
| 氏名の内容 | 半角スペースのみ / 全角スペースのみ / タブのみ / 前後に空白+有効文字 / 空 | F-09。trim 処理が無い |
| 他の必須項目 | 正常値のみ | 氏名だけを変数にするため固定 |

- 禁則: なし
- 導出に使う技法: 同値分割
- 導出したケース数: 5

### CH-05 (R-09 / 料金への反映漏れ)

| 因子 | 水準 | 切り出しの根拠 |
|---|---|---|
| 変更するフィールド | 人数 / 宿泊数 / 追加プラン | F-11。料金に影響する3因子 |
| 変更タイミング | 初回入力時 / 確認画面から戻って変更 | F-07 と同じ経路で古い値が残る懸念 |
| 確認する場所 | 入力画面の即時表示 / 確認画面の合計額 | 両者がずれる可能性 |

- 禁則: なし
- 導出に使う技法: ペアワイズ
- 導出したケース数: 6

### CH-06 (R-10 / 二重予約)

| 因子 | 水準 | 切り出しの根拠 |
|---|---|---|
| 操作 | 完了ダイアログを閉じて再クリック / 連打 / 別タブから同一内容を送信 | F-20。ボタンが無効化されない |
| 間隔 | 即時 / 数秒後 | 送信中の無効化有無を切り分けるため |

- 禁則: 別タブ × 即時 は再現性が低いため除外
- 導出に使う技法: デシジョンテーブル
- 導出したケース数: 6

---

## 実行順を入れ替えた理由

| CH-ID | 本来の順位 | 実際の順位 | 理由 |
|---|:--:|:--:|---|
| CH-01〜04 | リスクレベル同率 | 1〜4 | 根拠が同じ(3-2 + 3-6)ため、**問題の見えやすさ順**に確定(抜け落ち候補 R6 への回答)。客が気づかないものから先に見る |
| CH-07 | 標準の先頭 | 7 | 深いチャーター(CH-01〜06)の後。同じ BF-01 の CH-08 と連続させた |
| CH-12 | 浅い | 12 | 同じ BF-01 の CH-10・CH-11 と連続させ、セットアップの重複を避けた |

深いブロック → 標準ブロック → 浅いブロックの順。各ブロック内では同一 BF を連続させている。

---

## 意図的に対象外

`risk-register.md` の対象外リスクを理由付きで転記する。
「やらない」のか「浅く・後回し」なのかを区別して書く。

| R-ID | リスク | 扱い | 理由 |
|---|---|---|---|
| R-12 | レイアウト崩れ・文言の不備 (観点01) | **対象外** | 影響度 L × 発生可能性 L。F-19 で375/768/1920 いずれも問題なし |
| R-13 | 特殊文字・サニタイズ不足 (観点03) | **対象外** | 発生可能性 L(F-10 で検証済み)。損失カテゴリ 3-4(個人情報)は 2026-09-23 に ○(許容)と判定済み |
| R-14 | 同時実行・割込み (観点19,20,21,22) | **N/A** | 会員機能・ログイン・セッションが存在しない (F-21)。該当操作が無い |
| R-15 | NaN 送信時に1泊扱い (観点08) | **後回し** | JS 経由でしか再現せず実ユーザーの到達経路がない (F-12)。CH-02 でサーバ側チェックを確認する際にまとめて拾える |

---

## 実行の進め方

`Review-Status` を `Approved` にしてから、チャーター駆動モードで実行する。

```
/explore url=https://hotel-example-site.takeyaqa.dev/ja/plans.html charter=qa-knowledge/targets/hotel-example-site-takeyaqa-dev/test-charters.md
```

`/explore` はこのファイルの**実行順・深さ・ケース数・狙う欠陥仮定に従い**、
チャーターに無い観点には手を出さない(観点リストの優先度 P0→P3 は使わない)。
`Approved` でなければ探索を始めずに停止する。

各イテレーションで「状態」欄が更新される:

| 状態 | 意味 |
|---|---|
| `未実行` | まだ着手していない |
| `実行中` | 着手済み |
| `完了` | **完了条件を満たした**。テストが落ちても、バグを起票できたなら完了 |
| `未確認のまま終了` | 完了条件を満たせなかった。理由を備考に残す |

セッション終了時、`未確認のまま終了` と `未実行` は**残存リスクとして報告される**
(紐づくリスクIDつき)。テストの成否ではなく、完了条件で判断する。
