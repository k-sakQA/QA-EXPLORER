---
name: qa-explorer-report
description: QA Explorer プロジェクト (https://github.com/k-sakQA/QA-EXPLORER) のテスト結果を、人間にとって読みやすい1枚のHTMLレポートに集約する。reports/ と qa-knowledge/ に蓄積された Findings、バグレポート、manual-tests、coverage、session-log を読み取り、検出バグ・手動テスト候補・実行したテスト内容を全対象横断で1ファイルにまとめる。ユーザーが「QA Explorer のレポートを出して」「テスト結果をHTMLにまとめて」「探索結果をレビュー用に整形して」「reports フォルダの中身を見やすく出して」のように依頼したとき、また `findings.md`・`coverage.md`・`session-log.md`・`bugs/` ディレクトリ・`manual-tests.md` を含むリポジトリで結果サマリーを求められたときに必ず使うこと。テスト実行後のキャッチアップ用に最適化されている。
---

# QA Explorer Report Skill

QA Explorer (https://github.com/k-sakQA/QA-EXPLORER) のテスト結果を、シンプルで印刷もしやすい1枚のHTMLレポートに集約するためのskillです。

## このskillが対象とするリポジトリ構造

QA Explorer は以下の構造で結果を蓄積しています。このskillは、これらすべてを読み取って1枚のHTMLにまとめます。

```
qa-knowledge/
  targets/<target-slug>/
    findings.md              # Finding/Hypothesis/Probe の3層蓄積
    derived-viewpoints.md    # 派生観点
reports/
  <target-slug>/
    coverage.md              # 観点消化状況
    session-log.md           # セッション経緯 (Iteration 単位の記録)
    manual-tests.md          # 手動テスト候補 (存在する場合のみ)
    bugs/
      YYYY-MM-DD-viewpoint-NN-<slug>.md
tests/generated/
  *.spec.ts                  # Playwright spec
```

`<target-slug>` は対象URLのホスト名から生成された短い識別子（例: `hotel-example-site-takeyaqa-dev`)。複数の対象が並ぶこともある。

## ワークフロー

### Step 1: 対象を特定する

`reports/` 配下にあるすべての `<target-slug>` ディレクトリを列挙する。`_template/` ディレクトリは除外。

```bash
ls reports/ | grep -v '^_template$'
```

各 target-slug について、以下のファイルを読みに行く。

### Step 2: 各対象から情報を抽出する

target-slug ごとに、以下を読み取る。**ファイルが無い場合はスキップして空セクションとして扱う**(エラーにしない)。

| ファイル | 抽出する情報 |
|---|---|
| `reports/<slug>/bugs/*.md` | バグタイトル(H1)、Severity、Finding ID、観点、サマリー(冒頭2-3文)、**再現手順**、**ファイルパス(リンク用)** |
| `qa-knowledge/targets/<slug>/findings.md` | **Hypothesis 全件** (Statement, Status, Born from, Notes)、**Probe 全件** (Verifies, Plan, Result, Status)、**Needs-Human: Yes の Finding**、Severity 補完用の Source/Viewpoint |
| `reports/<slug>/manual-tests.md` | 手動テスト候補(MT-ID, 優先度, タイトル, ファイル内アンカー)。ファイルが無ければ「起票なし」と表示 |
| `reports/<slug>/coverage.md` | 観点表(No, 観点, 優先度, 状態, 備考) |
| `reports/<slug>/session-log.md` | Iteration ごとのテスト記録、次セッションへの申し送り |
| `tests/generated/*.spec.ts` | spec のコメント(/** ~ */)から「狙うバグ」「手順」を補完 |

### Step 3: 「実行したテストの内容」をまとめる

これは2ソースを組み合わせる:

- **session-log.md の各 Iteration 記述** から「観点」「結果」「新規Finding/Hypothesis」を取る
- **対応する spec.ts のコメント** から「テスト意図」「手順」「期待値」を補完する

session-log の Iteration N と spec ファイルは、観点番号 (`viewpoint-NN-*.spec.ts` や `probe-YYYYMMDD-NN-*.spec.ts`) で対応付ける。1つの Iteration に1ブロック、`<details>` で折りたためるカードにまとめる。

各テストカードに含めるフィールド:
- **テスト意図** (spec のコメントから / 無ければ session-log の "狙い" から要約)
- **手順** (spec の test ブロックから抽出した操作を箇条書き / 細かすぎる場合は3-5ステップに要約)
- **期待値** (spec の expect 文や「期待:」コメントから)
- **結果** (session-log の Iteration 結果。FAIL / PASS / REJECTED で色分け)

### Step 4: HTMLを生成する

`reports/_template/report-template.html` をベースに、抽出した情報を埋め込む
(テンプレートは各セクション1件ずつのサンプル入り参照実装。CSS・構造はそのまま使い、
データ部分だけ差し替える)。テンプレートの構造は以下:

1. **ヘッダー** — タイトル + 生成日
2. **全体サマリー** — 4カード(検出バグ / 仮説 C・R・O 内訳 / 人間への依頼 / 平均カバレッジ)
3. **対象セクション** (対象ごとに繰り返し)
   - **A. テスト担当者(AI)からの所見** — Confirmed な Hypothesis の Statement/Notes を素材に、
     「このサイトのどこが弱いか / 何が堅牢だったか / 次に人手で見るべきこと」を3〜5行の文章で。
     バグの羅列ではなく、テスト担当者として人間に語りかける示唆を書く(このスキルの主眼)
   - **B. 検出バグ一覧** — **バグとして起票したものだけの独立セクション**。
     各行のタイトルは `reports/<slug>/bugs/<file>.md` への**相対リンク**にする
     (レポートはプロジェクトルート保存なので相対パスでそのまま開ける)。
     概要欄にはバグレポートから抽出した**再現手順を `<details>` で折りたたみ埋め込み**し、
     ブラウザ内だけでも即確認できるようにする
   - **C. 発見 → 仮説 → 検証 → 帰結** — Hypothesis ごとに1カード。
     Confirmed はタイムライン形式(Born from の Finding → Statement → Probe Plan/Result →
     起票バグへのリンク + Notes から得た「新たな気づき」)。
     Rejected はコンパクト形式で「誤検出の原因 = テスト設計の学び」を必ず書く。
     Open は「検証待ち」として D にも再掲する
   - 観点カバレッジ表
   - 実行したテストの内容(`<details>`)
   - **D. 次の一手(人とAIの分担)** — **対象セクションの最後**に置く。3行のテーブル:
     「人間に依頼」(Needs-Human: Yes の Finding を質問形式で) /
     「AIが次回検証」(Open Hypothesis・Planned Probe) /
     「手動テスト候補」(manual-tests.md、無ければ起票なし)
4. **フッター** — `Generated by QA-Explorer(Presented by Kaz Sakata)  |  YYYY-MM-DD`

#### Severity と優先度の色分け対応

Severity と優先度はテンプレート内のCSSクラスに対応している:

- バグ Severity: `critical` / `high` / `medium` / `low` (バグレポートの「深刻度」「Severity」欄から取得。書かれていなければ `medium` をデフォルトに)
- 手動テスト 優先度: `high` / `medium` / `low`
- カバレッジ状態: `done` (消化済み/再消化済み/手動テスト完了) / `recheck` (再訪推奨/手動テスト待ち) / `todo` (未着手/実施中) / `na` (N/A)

#### 観点カバレッジ表に含める範囲

23観点すべてではなく、**「未着手以外」の観点 + 主要なN/A** を表示する。23行は冗長すぎる。ただし「未着手」の観点が多く残っている場合は、表の下に「他に N観点 が未着手」と注記する。

### Step 5: ファイルを保存して提示する

ファイル名は `qa-explorer-report-YYYYMMDD.html` 形式で、プロジェクトルートに保存する。

## 出力先

- 最終出力: `qa-explorer-report-YYYYMMDD.html`（プロジェクトルート）

## やらないこと

- **バグの修正提案をレポートに混ぜない**。所見(A)と気づき(C)は findings.md /
  bugs/ に記録された事実・仮説・検証結果**のみ**を素材に書き、記録に無いことを
  推測で補わない。QAは原因を見つけて報告するのが仕事であり、修正は開発者の役割。
- **観点リスト(viewpoints.md)の書き換えや派生観点の昇格判断はしない**。
- **テスト結果の評価コメント(良い/悪い)を勝手に追加しない**。事実(FAIL/PASS/REJECTED)だけを淡々と記載する。
- **ファイルが見つからないときに架空のデータで埋めない**。空セクションは「起票なし」と明示する。

## 参考ファイル

- `reports/_template/report-template.html` — HTMLレポートのテンプレート(完全な参照実装)。
  配色・レイアウト・新セクションA〜D・印刷用CSSを含む。新しいレポートを作るときは、
  このファイルをコピーして中身のデータ部分だけ差し替える。
